        Matching algorithm matches the minutiae features from the image template and the features extracted from the image acquired through sensor in a pixel by pixel manner. If the output is 1 then both the features are from the same person or 0 if both the features are fromm the different persons.
